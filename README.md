# woocommerce

```
docker-compose up -d
docker-compose logs -f

# run & install
http://localhost:8000

# To shutdown
docker-compose down

# To shutdown and delete volumes
docker-compose down --volumes
```
